搭建一个自动化文档信息提取流水线，用于批量识别数万份公司年报各自的董事、监事和高级管理人员，并按照在年报中名字出现的先后顺序排序。
由于输入的是PDF，格式不稳定（有可能是表格，也可能是文字），且边缘情况较多，所以只能依靠大模型进行核心分析。
又由于页数很长（单PDF可能上百页），直接交付大模型几乎肯定会出错，因此需要一个处理管线，首先进行PDF分块，并引入先验知识，编写处理管线分模块编排。

---

大致设计如下：
输入：PDF
处理流水线：

编号 功能 方案 技术组合 目标
1 目录提取 顺序逐页OCR，并判断 粗查 目录页
2 目录解析 目录页解析，判断目标范围 精查 董监高章节起止页码
3 处理阈截取 截取指定页数范围生成处理目标(董监高章节) 粗查 董监高节选
4 块截取 在董监高章节分块存储 精查 分块的内容，结构化的表格和文字
5 反查名 用“董监高”及变体关键词反查所有包含块，进一步得到重要人物名单 分析 重要人物名单
6 查名 逐个使用重要人物名单，查询所有包含块，进一步分析身份 分析和结构化输出 人物-身份表
7 总结 总结出身份-人物列表 简单算法 身份-人物表
输出 身份-人物表

暂时不涉及反馈纠正

模型选型：
粗查：OCR+gpt4o
精查：PP-StructureV3+gpt4o
分析： gpt4o

我们先完成核心MVP：4、5、6步
只要这里没问题，那其他都很简单。
为我编写第四步的脚本，输入一个PDF，存储块列表